{
  "name": "tech-stack-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "audit:report": "node scripts/serve-impeccable-audit-report.mjs",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "bulk:api:dev": "tsx watch bulk/api/src/server.ts",
    "bulk:api:start": "tsx bulk/api/src/server.ts",
    "bulk:ingest": "tsx bulk/ingest/src/cli.ts",
    "bulk:import": "tsx bulk/import/src/cli.ts",
    "bulk:load": "tsx bulk/load/src/cli.ts",
    "bulk:ingest:compose": "docker compose --env-file .env.bulk exec -T bulk-api npm run bulk:ingest --",
    "bulk:import:compose": "docker compose --env-file .env.bulk exec -T bulk-api npm run bulk:import --",
    "bulk:load:compose": "docker compose --env-file .env.bulk exec -T bulk-api npm run bulk:load --",
    "bulk:health:compose": "docker compose --env-file .env.bulk exec -T bulk-api node -e \"const port = process.env.HONO_PORT || '8787'; fetch('http://127.0.0.1:' + port + '/health').then(async (response) => { const body = await response.text(); if (!response.ok) { console.error(body); process.exit(1); } process.stdout.write(body); }).catch((error) => { console.error(error instanceof Error ? error.message : String(error)); process.exit(1); });\"",
    "bulk:compose:up": "docker compose --env-file .env.bulk up -d --build",
    "bulk:compose:down": "docker compose --env-file .env.bulk down"
  },
  "dependencies": {
    "@carbon/icons-react": "^11.80.0",
    "@clickhouse/client": "^1.18.5",
    "@duckdb/node-api": "^1.5.2-r.1",
    "@hono/node-server": "^2.0.2",
    "@radix-ui/react-slot": "^1.1.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "framer-motion": "^12.38.0",
    "hono": "^4.12.18",
    "lucide-react": "^0.468.0",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^2.5.5",
    "tsx": "^4.22.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "autoprefixer": "^10.4.20",
    "eslint": "^9.17.0",
    "eslint-config-next": "^15.1.0",
    "jsdom": "^25.0.1",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
